- python3.11 설치하고, 가상환경 설정
- `pip install -r requirements.txt`
- `git config --global credential.helper store`
- `hf auth login`

```bash
export HF_HUB_ENABLE_HF_TRANSFER=1
```

- `download.py` 사용해 모델 다운로드
