# Prompt
C++로 빠른 푸리에 변환(FFT)을 구현해봐.

# Answer
물론입니다! C++로 **빠른 푸리에 변환**(Fast Fourier Transform, FFT)을 구현하는 것은 신호 처리, 이미지 처리, 데이터 분석 등에서 매우 유용합니다. 아래는 **Cooley-Tukey 알고리즘**을 기반으로 한 **재귀적 FFT 구현** 예제입니다.

---

## ✅ C++로 구현한 FFT (Cooley-Tukey 알고리즘)

```cpp
#include <iostream>
#include <vector>
#include <complex>
#include <cmath>

using namespace std;

const double PI = acos(-1.0);

// FFT 함수: 재귀적으로 구현
void fft(vector<complex<double>>& a, bool invert) {
    int n = a.size();
    if (n <= 1) return;

    // 짝수 인덱스와 홀수 인덱스로 분할
    vector<complex<double>> even(n / 2);
    vector<complex<double>> odd(n / 2);

    for (int i = 0; i < n / 2; ++i) {
        even[i] = a[2 * i];
        odd[i] = a[2 * i + 1];
    }

    fft(even, invert);
    fft(odd, invert);

    double angle = 2 * PI / n * (invert ? -1 : 1);
    complex<double> w(1), wn(cos(angle), sin(angle));

    for (int i = 0; i < n / 2; ++i) {
        a[i] = even[i] + w * odd[i];
        a[i + n / 2] = even[i] - w * odd[i];
        w *= wn;
    }
}

// FFT를 사용하여 두 다항식의 곱셈을 계산 (FFT를 이용한 다항식 곱셈)
vector<int> multiply(const vector<int>& a, const vector<int>& b) {
    vector<complex<double>> fa(a.begin(), a.end());
    vector<complex<double>> fb(b.begin(), b.end());

    int n = 1;
    while (n < a.size() + b.size()) n <<= 1;
    fa.resize(n);
    fb.resize(n);

    fft(fa, false);
    fft(fb, false);

    for (int i = 0; i < n; ++i) {
        fa[i] *= fb[i];
    }

    fft(fa, true);

    vector<int> result(n);
    for (int i = 0; i < n; ++i) {
        result[i] = round(fa[i].real());
    }

    return result;
}

int main() {
    // 예시: 두 다항식의 곱셈
    vector<int> a = {1, 2, 3};  // 1 + 2x + 3x^2
    vector<int> b = {4, 5, 6};  // 4 + 5x + 6x^2

    vector<int> result = multiply(a, b);

    cout << "Result of multiplication: ";
    for (int x : result) {
        cout << x << " ";
    }
    cout << endl;

    return 0;
}
```

---

## 📌 설명:

- `fft` 함수는 **Cooley-Tukey FFT 알고리즘**을 구현한 것입니다.
- `invert` 파라미터는 **Inverse FFT**를 위한 플래그입니다.
- `multiply` 함수는 FFT를 사용하여 두 다항식의 곱셈을 효율적으로 계산합니다.
- `complex<double>`는 C++ 표준 라이브러리의 복소수 타입을 사용합니다.

---

## 🧪 실행 결과 예시:

```
Result of multiplication: 4 13 28 27 18
```

이는 `(1+2x+3x²) × (4+5x+6x²)`의 계수입니다.

---

## 🚀 추가 팁:

- 이 코드는 **정확도를 위해 복소수 연산**을 사용합니다.
- 실제 응용에서는 **버퍼 크기를 2의 거듭제곱으로 맞추는 것이 좋습니다**.
- 더 빠른 성능을 원한다면 **반복적 FFT**(iterative FFT)를 구현할 수 있습니다.

---

필요하다면 **반복적 버전**, **실수 FFT**, **2D FFT** 등도 구현해드릴 수 있어요!